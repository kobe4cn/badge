apiVersion: apps/v1
kind: Deployment
metadata:
  name: badge-admin-service
  namespace: badge-system
  labels:
    app.kubernetes.io/name: badge-admin-service
    app.kubernetes.io/part-of: badge-system
    app.kubernetes.io/component: api-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: badge-admin-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: badge-admin-service
        app.kubernetes.io/part-of: badge-system
        app.kubernetes.io/component: api-gateway
    spec:
      serviceAccountName: badge-service-account
      terminationGracePeriodSeconds: 30
      containers:
        - name: badge-admin-service
          image: badge-system/badge-admin-service:latest
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: badge-config
            - secretRef:
                name: badge-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
