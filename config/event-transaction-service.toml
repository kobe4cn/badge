# Event Transaction Service 配置
# 交易事件处理服务 - gRPC

[server]
port = 50054

[kafka]
consumer_group = "event-transaction-service"

[rules]
refresh_interval_secs = 30
initial_load_timeout_secs = 10
idempotency_ttl_hours = 24
