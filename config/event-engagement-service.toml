# Event Engagement Service 配置
# 互动事件处理服务 - gRPC

[server]
port = 50053

[kafka]
consumer_group = "event-engagement-service"

[rules]
refresh_interval_secs = 30
initial_load_timeout_secs = 10
idempotency_ttl_hours = 24
